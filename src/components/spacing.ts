/**
 * Shared chat-surface spacing tokens.
 *
 * `BUBBLE_X_PADDING` is the horizontal inset used by every chat bubble (the
 * counselor and assistant text bubbles). The recommended-colleges card and its
 * rows reference the same token so their content lines up with the message text
 * instead of drifting to its own inset.
 */
export const BUBBLE_X_PADDING = "px-[18px]";
